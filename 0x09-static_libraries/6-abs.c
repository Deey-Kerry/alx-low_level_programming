#include "main.h"
/**
 * _abs - It computes the absolute value of an integer
 *
 * @i: input
 *
 * Return: absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}

	else
	{
		return (i * -1);
	}
}
